import requests
import json
import main

'''if __name__ == '__main__':
    main.app.run(debug=True)

    # base for all urls
    url = 'http://localhost:5000'

    # users to enter
    #users_all = [{'name': 'Ody'}, {'name': 'Mo'}, {'name': 'Albus'}, {'name': 'Shannon'}]
    #for user in users_all:
    requests.post('http://localhost:5000/users', json={'name': 'foo'})


    # messages to 'send'
'''


